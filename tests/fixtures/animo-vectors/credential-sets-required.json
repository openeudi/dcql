{
  "query": {
    "credentials": [
      { "id": "pid", "format": "dc+sd-jwt" },
      { "id": "photo_id", "format": "mso_mdoc" }
    ],
    "credential_sets": [{ "options": [["pid"], ["photo_id"]], "required": true }]
  },
  "credentials": [{ "id": "cred-pid", "format": "dc+sd-jwt", "claims": {} }],
  "expected": { "satisfied": true, "submission": { "pid": "cred-pid" } }
}
